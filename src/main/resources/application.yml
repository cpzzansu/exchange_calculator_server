spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exchange?allowPublicKeyRetrieval=true&useSSL=true&serverTimezone=UTC
    username: exchange
    password: exchange!234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      iptime:
        cpzzansu:
          exchange_calculator_server: debug
    root: info